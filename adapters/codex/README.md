# Codex Adapter

Treat root `AGENTS.md` as the canonical project engineering policy.

Load domain Playbooks only when relevant—for example, `database.md` for database work or `distributed-systems.md` for distributed coordination.

Do not maintain a separate Codex-specific Core Policy.
