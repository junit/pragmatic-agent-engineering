# Simplification Cases

This directory is for real cases where removing complexity improved the system without weakening important guarantees.

A useful case should explain:

- what problem the original design solved;
- which complexity later proved unnecessary;
- which invariants and protections remained;
- how the simplified design was verified.

Deleting code is not the goal by itself. **Reducing low-value complexity while preserving real capability is.**
